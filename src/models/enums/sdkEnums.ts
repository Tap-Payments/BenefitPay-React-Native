export enum SdkMode {
  production,
  sandbox,
}

export enum Locale {
  en = 'en',
  ar = 'ar',
}

export enum ColorStyle {
  monochrome = 'monochrome',
  colored = 'colored',
}
export enum Theme {
  light = 'light',
  dark = 'dark',
}

export enum Edges {
  curved = 'curved',
  flat = 'flat',
}
